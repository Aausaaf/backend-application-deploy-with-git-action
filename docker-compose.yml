services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        restart: unless-stopped
        ports:
            - "7080:7080"
        environment:
            - PORT=7080
